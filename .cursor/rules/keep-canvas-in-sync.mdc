---
description: Keep Cursor canvases and static HTML in sync with property and analysis data
alwaysApply: true
---

# Canvases are the primary reference

The user reads **canvases first**. Whenever facts change, update every surface that shows those facts **in the same response**, then **one commit** that includes both data files and canvases.

## Authoritative inputs (touch these → sync downstream)

| Source | Role |
|---|---|
| `index.html` | PWA: `PROPERTIES`, visits, watch list, floor plans |
| `analysis.html` | Static multi-page export of the same narrative (shortlist, areas, builders, legal, etc.) |

**Rule:** If you edit **`index.html`** or **`analysis.html`** for shortlist, price, possession, RERA, commute, areas, or builders — update the canvases in the table below in the **same turn**, then commit **HTML + canvases together**.

**Repo path for canvases (commit here):** `canvases/*.canvas.tsx` under the project root (e.g. `c:\Users\jaeck\Github\drive-empty\canvases\`).

**Cursor UI path (picker beside chat):** `%USERPROFILE%\.cursor\projects\<workspace-folder>\canvases\` — e.g. `C:\Users\jaeck\.cursor\projects\c-Users-jaeck-Github-drive-empty\canvases\`. Cursor may show canvases from this folder even if they were removed from git. After deleting or renaming a `.canvas.tsx` in the repo, **delete the same filename there** and **copy** `canvases\*.canvas.tsx` from the repo into that folder so the canvas list matches git.

---

## Canvas map (what to update when)

### Mirrors shortlist / visits / areas / money

| File | Covers | When to update |
|---|---|---|
| `blr-property-shortlist.canvas.tsx` | Ranked 5 properties, scores, guardrails | Any add/remove/price/possession/RERA/commute |
| `blr-visit-guide.canvas.tsx` | Itinerary, contacts, drive times | Same as shortlist + visit logistics |
| `blr-areas.canvas.tsx` | 5 core areas + watch-list pins (Manyata, Birla Yelahanka, Navaratna Agrahara, Aerospace Bagalur, IVC); livability, connectivity, social, suitability | Area facts, watch-list text, shortlist geography |
| `blr-investment.canvas.tsx` | Criterion 3 — CAGR, PSF, yield, catalysts, return framing | Price, possession, area thesis, infra assumptions |
| `blr-budget-financial-planning.canvas.tsx` | All-in, EMI, CLP, opportunity cost | Price, rate, timeline, budget ceiling |
| `blr-your-budget-profile.canvas.tsx` | Personal budget / eligibility | User budget inputs or ceiling policy |
| `blr-purchase-strategy.canvas.tsx` | Booking, negotiation, CLP | Strategy or shortlist-driven timing |

### Builder + evaluation spine

| File | Covers | When to update |
|---|---|---|
| `blr-builder-reputation.canvas.tsx` | **Citywide** BLR builders (tier, scores, corridors), North shortlist callouts | New builder on radar, tier/score edits, **any** shortlist builder row change |
| `blr-deep-eval.canvas.tsx` | 10-criterion matrix per property (no duplicate “expanded area” narrative — that is `blr-areas` only) | **Any** property fact change **or** finishing a criterion write-up |
| `blr-full-evaluation.canvas.tsx` | Cross-criterion summary | Whenever deep-eval or a criterion canvas materially changes |
| `blr-property-criteria.canvas.tsx` | Framework definitions | Only if the 10-point framework text changes |
| `blr-project-selection.canvas.tsx` | Five-filter stack (budget → area → builder → amenities → other) on the **visit shortlist** | Shortlist membership, all-in bands, deep-eval verdicts/scores that change pick order |
| `blr-research-snapshot.canvas.tsx` | Web vs repo log; Reddit diligence threads (North BLR / UC / builder practices) + shortlist + peers/watch | After **external research** (portals, Reddit, etc.) or major portal vs repo conflicts |
| `blr-shortlist-ten.canvas.tsx` | **Fixed list of 10** projects (5 core visits + 5 expand) for research passes | Any change to which ten are in-scope; then refresh research-snapshot pass #2 notes and, if visits expand, index + deep-eval |

### Criterion deep dives (keep aligned with `blr-deep-eval`)

| File | Criterion | When to update |
|---|---|---|
| `blr-property-type-config.canvas.tsx` | **4** — type, density, **UDS**, 5-yr hold strategy (not floor-by-floor picks) | Strategy thesis change; hold/UDS narrative |
| `blr-legal-documentation.canvas.tsx` | **5** — UC booking vs handover, BBA, KRERA, Khata/OC path | Legal process, RERA, UC vs RTM wording |
| `blr-amenities.canvas.tsx` | **6** — project amenities | Specs / brochure amenities change |
| `blr-safety-lifestyle.canvas.tsx` | **9** — risk, environment | Risk or area hazard data |
| `blr-society-maintenance.canvas.tsx` | Maintenance / sinking fund | Society fee or structure data |

---

## Minimal change checklist (copy mentally)

**Property added/removed or renamed**  
→ shortlist · visit-guide · areas · investment · budget-financial · **builder-reputation** (north row) · **deep-eval** · **project-selection** · full-evaluation (if present) · amenities/safety if that property had rows

**Price, all-in, possession, RERA, commute, address**  
→ shortlist · visit-guide · investment · budget-financial · **deep-eval** (all affected cells) · **project-selection** · **research-snapshot** (if present — update “web vs repo” rows) · builder canvas only if narrative mentions that project

**Area / corridor thesis (e.g. STRR, new micro-market)**  
→ areas · investment · property-type-config (if hold/UDS story) · deep-eval area/connectivity/investment cells

**Builder landscape (new Tier 1/2, score edits, citywide note)**  
→ **builder-reputation** only (unless shortlist assigns a new builder — then also shortlist + deep-eval)

**Finishing a criterion write-up**  
→ the criterion canvas **and** matching cells in **deep-eval** (same response)

---

## Hard rules

0. **One area canvas only:** `blr-areas.canvas.tsx` is the single source for area selection, livability, connectivity, watch pins, and suitability. Do **not** recreate `blr-area-livability`, `blr-area-selection`, `blr-connectivity`, or `blr-area-single-adult` as separate files.

1. **Never commit HTML shortlist changes without canvas updates** in the same commit (unless the change is purely CSS/typos with zero factual delta).
2. **Never** mark a criterion “done” in chat without updating **`blr-deep-eval.canvas.tsx`** for that criterion.
3. If unsure which canvases apply, **update more** rather than skip — stale canvases waste the user’s time.
4. **`analysis.html`** and **`index.html`** must not diverge on the same facts; if both exist, patch **both** or explicitly leave one unchanged with a one-line reason in the commit body.
